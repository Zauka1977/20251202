plugins {
    id 'com.android.application'
    id 'kotlin-android'
}

android {
    namespace 'com.example.handball' // <- EZ AZ UTOLSÓ, HIÁNYZÓ SOR!
    compileSdk 34 
    // ... (a többi config) ...
}

dependencies {
    // ... (a többi dependency) ...
}
